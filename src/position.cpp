#include "position.h"

Position::Position(int row, int column) {       // implementando os atributos definidos no header position.h
    this -> row = row;                          // o método construtor definira o atributo row como o row passado como parametro
    this -> column = column;                    // mesma coisa para o column