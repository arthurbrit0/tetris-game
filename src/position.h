#pragma once

class Position {
    public:
        Position(int row, int column);  // header que define o metodo construtor da classe Position
        int row;                        // tambem define os atributos de row e column
        int column;
};